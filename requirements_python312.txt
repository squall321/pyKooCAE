# pyKooCAE Requirements - Python 3.12
# Windows 환경에서 가상환경 설정 시 사용
# 생성일: 2026-01-29

# ============================================================================
# 핵심 과학 계산 라이브러리
# ============================================================================
numpy==2.4.1
scipy==1.17.0
matplotlib==3.10.6
pandas==2.3.3
scikit-learn==1.7.2
scikit-image==0.25.2

# ============================================================================
# 3D 처리 및 시각화
# ============================================================================
vtk==9.5.2
trimesh==4.8.3
numpy-stl==3.2.0
opencv-python==4.13.0.90  # Windows에서는 opencv-python-headless 대신 사용 가능

# ============================================================================
# GUI 프레임워크
# ============================================================================
PyQt5==5.15.11
# PyQt5-Qt5==5.15.17  # Linux 전용 - Windows에서는 PyQt5에 Qt 바이너리 포함됨
PyQt5-sip==12.17.0

# ============================================================================
# 3D CAD 라이브러리 (PythonOCC)
# ============================================================================
# 주의: PythonOCC는 pip로 직접 설치가 어려울 수 있습니다.
# Windows에서는 다음 방법 중 하나를 사용하세요:
#
# 방법 1: conda-forge (권장)
#   conda install -c conda-forge pythonocc-core=7.9.0
#
# 방법 2: 사전 빌드된 wheel 파일
#   https://github.com/tpaviot/pythonocc-core/releases
#
# 방법 3: 수동 빌드 (고급)
#   OpenCascade 7.9.0 소스에서 빌드

# ============================================================================
# 빌드 도구
# ============================================================================
nuitka==2.8.10
# Windows에서 Nuitka 사용 시 추가 필요:
# - Microsoft Visual Studio Build Tools
# - Windows SDK

# ============================================================================
# 웹 시각화 및 대시보드
# ============================================================================
plotly==6.3.1
dash==3.2.0
kaleido==0.2.1

# ============================================================================
# Jupyter 및 대화형 환경
# ============================================================================
jupyter==1.1.1
ipython==9.6.0
ipykernel==7.1.0
ipywidgets==8.1.7
notebook==7.4.4

# ============================================================================
# 3D 처리 추가 도구
# ============================================================================
pyvista==0.45.3
pyvistaqt==0.12.1
pymeshlab==2024.12
open3d==0.19.0
vedo==2025.2.0

# ============================================================================
# 유틸리티 라이브러리
# ============================================================================
addict==2.4.0
tqdm==4.68.0
PyYAML==6.1.0
lxml==5.4.0
pillow==11.2.0
shapely==2.1.0
networkx==3.5.1
pyquaternion==1.1.4
ConfigArgParse==1.7.1

# ============================================================================
# 물리 시뮬레이션 (선택)
# ============================================================================
pybullet==3.2.6

# ============================================================================
# 개발 도구 (선택)
# ============================================================================
pytest==8.3.6
black==25.1.0
flake8==7.3.1
mypy==1.14.4

# ============================================================================
# 설치 순서 (Windows)
# ============================================================================
# 1. Python 3.12 설치
# 2. 가상환경 생성:
#    python -m venv venv312
#    venv312\Scripts\activate
#
# 3. pip 업그레이드:
#    python -m pip install --upgrade pip
#
# 4. 핵심 패키지 먼저 설치:
#    pip install numpy scipy matplotlib
#
# 5. PythonOCC 설치 (conda 사용 권장):
#    conda install -c conda-forge pythonocc-core=7.9.0
#
# 6. 나머지 패키지 설치:
#    pip install -r requirements_python312.txt
#
# 7. Nuitka 빌드를 위해 Visual Studio Build Tools 설치:
#    https://visualstudio.microsoft.com/downloads/
#    - "Desktop development with C++" 워크로드 선택
